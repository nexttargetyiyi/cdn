name: 定时 Ping 伪装域名

on:
  schedule:
    - cron: '0 * * * *' # 每小时执行一次
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: 模拟访问首页
        run: |
          curl -s https://cdn.dw-j.sbs > /dev/null
          curl -s https://cdn.dw-j.sbs/blog > /dev/null
          curl -s https://cdn.dw-j.sbs/contact > /dev/null
